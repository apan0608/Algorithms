using Problems.Problem;


namespace Problems.ProblemGenerator {
    public  class FindingPairsGenerator: IProblemGenerator {

        // if explicitly implements an interface, the method will be forced to use private as return type 
        public IProblem GenerateProblem(string[] inputs)
        {
            throw new System.NotImplementedException();
        }

    }
}